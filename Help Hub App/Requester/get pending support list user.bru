meta {
  name: get pending support list user
  type: http
  seq: 2
}

post {
  url: {{base_url}}/UserPendingList
  body: json
  auth: inherit
}

body:json {
  {
      "empid": "u0001"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
