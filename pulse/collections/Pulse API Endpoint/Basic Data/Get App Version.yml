info:
  name: Get App Version
  type: http
  seq: 11

http:
  method: GET
  url: "{{pulse_api}}/Basic/get-appVersion"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
