meta {
  name: /api/v1/external/order-confirmation-mail
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/external/order-confirmation-mail
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "orderNumber": "string"
  }
}

settings {
  encodeUrl: true
}

example {
  name: OK
  
  request: {
    url: {{baseUrl}}/api/v1/external/order-confirmation-mail
    method: POST
    mode: json
    headers: {
      Content-Type: application/json
      Authorization: Bearer <token>
    }
  
    body:json: {
      {
        "orderNumber": "string"
      }
    }
  }
  
  response: {
    status: {
      code: OK
      text: 200
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
