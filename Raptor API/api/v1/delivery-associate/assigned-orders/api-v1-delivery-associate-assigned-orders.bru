meta {
  name: /api/v1/delivery-associate/assigned-orders
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/delivery-associate/assigned-orders
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "deliveryAssociateId": "string"
  }
}

settings {
  encodeUrl: true
}

example {
  name: OK
  
  request: {
    url: {{baseUrl}}/api/v1/delivery-associate/assigned-orders
    method: POST
    mode: json
    headers: {
      Content-Type: application/json
      Authorization: Bearer <token>
    }
  
    body:json: {
      {
        "deliveryAssociateId": "string"
      }
    }
  }
  
  response: {
    status: {
      code: OK
      text: 200
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
