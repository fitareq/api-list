info:
  name: ApproveAll
  type: http
  seq: 3

http:
  method: POST
  url: "{{pulse_api}}/SpecialSales/ApproveAll"
  body:
    type: json
    data: "{\r\n    \"UserId\": \"\"\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
