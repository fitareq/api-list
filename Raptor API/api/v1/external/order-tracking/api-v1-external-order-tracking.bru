meta {
  name: /api/v1/external/order-tracking
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/external/order-tracking?orderNumber=string
  body: none
  auth: inherit
}

params:query {
  orderNumber: string
}

settings {
  encodeUrl: true
}

example {
  name: OK
  
  request: {
    url: {{baseUrl}}/api/v1/external/order-tracking?orderNumber=string
    method: GET
    mode: none
    params:query: {
      orderNumber: string
    }
  
    headers: {
      Authorization: Bearer <token>
    }
  }
  
  response: {
    status: {
      code: OK
      text: 200
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
