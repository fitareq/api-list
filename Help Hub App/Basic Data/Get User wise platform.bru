meta {
  name: Get User wise platform
  type: http
  seq: 1
}

post {
  url: {{base_url}}/GetPlatformsList
  body: json
  auth: inherit
}

body:json {
  {
      "rpm_emp_code": "01250"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
