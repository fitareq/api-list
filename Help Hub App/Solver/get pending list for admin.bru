meta {
  name: get pending list for admin
  type: http
  seq: 1
}

post {
  url: {{base_url}}/AdminPendingList
  body: json
  auth: inherit
}

body:json {
  {
      "empid": "31246"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
